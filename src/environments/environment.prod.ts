export const environment = {
  production: true,
  title: 'Prod Environment Heading',
  budgetUrl: 'http://5.181.217.203:8080/api/v1/budgets'
};


// ng build

// ng build --configuration=production

// ng build --configuration=dev
